# web

SvelteKit 5 frontend for Rakda. Uses **Bun**.

```sh
bun install
bun run dev      # http://localhost:5173
bun run build
bun run check    # svelte-check
bun run lint     # prettier --check && eslint
```

Architecture, auth pattern and UI rules: `../CLAUDE.md`, `PRODUCT.md`, `DESIGN.md`.
